const words = ["spray", "elite", "exuberant", "destruction", "present"];

const result = words.filter((myWord) => myWord.length > 6);  // any myWord.length is greater then 6 that will return 

console.log(result);
// Expected output: Array ["exuberant", "destruction", "present"]
